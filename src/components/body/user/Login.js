import React, {} from 'react';

function Login() {

  return (
    <div>
      {/* {state.user.name ? `Welcome ${state.user.name}!...`: 'Please login first. :)'} */}
    </div>
  )
}

export default Login
